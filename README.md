

# Tracero
An agent focusing on correcting errors when accidents occur in autonomous driving and robotics

To run the backend locally, `cd first try/tracero_backend && uvicorn app:app --reload` and export `DEEPSEEK_API_KEY` for the reasoning endpoints.
